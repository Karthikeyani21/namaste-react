# testing

# React testing library - uses Jest
  - which build on top of DOM testing library
  - using create-react-app, we can get the react testing library by default 
  - 
# Jest - js testing framework


# install 
  - npm i -D @testing-library/react
  - npm i -D jest
      - to use jest along with babel we need to install more dependencies
      - npm install --save-dev babel-jest @babel/core @babel/preset-env
         - babel - transpiler which parcel uses